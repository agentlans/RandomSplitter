% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RandomSplitter.R
\name{split_df}
\alias{split_df}
\title{Split a Data Frame into Multiple Subsets}
\usage{
split_df(df, weights = c(0.8, 0.2), names = NULL, stratify_by = NULL)
}
\arguments{
\item{df}{A \code{data.frame} to split.}

\item{weights}{A numeric vector of weights summing to 1 (e.g., \code{c(0.7, 0.2, 0.1)}).
If the sum is not 1, weights are normalized automatically.}

\item{names}{Optional character vector of names for the resulting list.
Defaults to \code{c("train", "test")} if two weights are provided and names are NULL.}

\item{stratify_by}{Optional string. The name of the column to use for
stratified splitting (typically a categorical target variable).}
}
\value{
A named \code{list} of \code{data.frame} objects.
}
\description{
Divides a data frame into multiple parts (e.g., train, validation, test)
based on provided weights. Supports both simple random splitting and
stratified splitting to maintain class balances.
}
\details{
This function utilizes underlying Rcpp implementations (\code{random_split_indices}
or \code{stratified_split_indices}) for high-performance index generation.
}
\examples{
# 3-way split: 70\% Train, 20\% Val, 10\% Test
sets <- split_df(iris, weights = c(0.7, 0.2, 0.1), names = c("tr", "val", "te"))
lapply(sets, nrow)

# Stratified split to maintain Species proportions
stratified_sets <- split_df(iris, weights = c(0.8, 0.2), stratify_by = "Species")
}
